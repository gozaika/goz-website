/**
 * goZaika palette — now sourced from the framework-neutral `@gozaika/design-tokens`
 * package so native and web share one source of truth (and one set of AA-safe
 * accent-text companions). Re-exported here to keep `@gozaika/mobile-ui`'s public
 * API byte-compatible (`import { palette, toneColors, accents } from "../tokens/colors"`).
 */
export { palette, toneColors, accents, type StatusTone, type ToneColors } from "@gozaika/design-tokens";
