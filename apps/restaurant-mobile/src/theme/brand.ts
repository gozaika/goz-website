// Brand tokens now live in @gozaika/mobile-ui (Slice 2). Re-exported as `brand`
// for the route files written in Slice 1; new code should import `palette` directly.
export { palette as brand } from "@gozaika/mobile-ui";
