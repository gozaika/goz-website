import { Placeholder } from "@/ui/Placeholder";

export default function DashboardScreen() {
  return (
    <Placeholder
      title="Dashboard"
      subtitle="Restaurant status, today's revenue/sell-through/AOV, next drop and pickup-queue summary arrive in Mobile Slice 14. Role-adapted per membership."
      slice="Slice 14"
    />
  );
}
