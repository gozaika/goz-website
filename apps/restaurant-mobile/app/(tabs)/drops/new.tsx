import { Placeholder } from "@/ui/Placeholder";

// Mirrors the web portal's /portal/drops/new which consolidates create/edit/publish.
export default function NewDropScreen() {
  return (
    <Placeholder
      title="New drop"
      subtitle="Create-from-template form with schedule, price (paise), quantity and disclosure. Status transitions arrive in Mobile Slice 13."
      slice="Slice 13"
    />
  );
}
