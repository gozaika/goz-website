import { Placeholder } from "@/ui/Placeholder";

export default function LoginScreen() {
  return (
    <Placeholder
      title="Sign in"
      subtitle="Phone OTP + Google OAuth, then membership bootstrap (role, restaurant status, switcher). Arrives in Mobile Slice 6."
      slice="Slice 6"
    />
  );
}
