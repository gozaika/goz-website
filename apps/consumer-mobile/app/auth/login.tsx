import { Placeholder } from "@/ui/Placeholder";

export default function LoginScreen() {
  return (
    <Placeholder
      title="Sign in"
      subtitle="Phone OTP (primary) and Google OAuth (secondary) with SecureStore session arrive in Mobile Slice 6."
      slice="Slice 6"
      accent="#1A5C38"
    />
  );
}
