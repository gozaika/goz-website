# Expo + npm-workspaces: Expo SDK packages use tight peer ranges that npm's strict
# peer resolver rejects in a monorepo (e.g. expo-router -> @expo/log-box). This lets
# installs (incl. `expo prebuild`/`run:android`) proceed with Expo's intended graph.
legacy-peer-deps=true
