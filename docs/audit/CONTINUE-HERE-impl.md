# CONTINUE HERE — goZaika feature-parity implementation

**If you are a fresh session, read this first, then `IMPLEMENTATION-PLAN.md`, then the §0 build sequence in `business-model-audit.md`.**

## Session 7 progress (2026-07-08) — Phase 4 (CW-3) + Phase 5 (Playwright specs) DONE; PR opened for review
- **Phase 4 CW-3 real imagery — DONE (verify + one on-spec change).** Audited the brand-asset layer first: the canonical §25 marks are **already standardized across every surface** — `public/brand/gozaika-mark.svg` is **byte-identical** to canonical `icons/flame.svg` in consumer-web/restaurant-mgmt-web/admin-web; the horizontal + white logos are the **same art** as canonical `icons/gozaika-logo*.svg` (only mask-`id`/`aria-label`/comments differ); `public/brand/hero-bam-bag.webp` is **byte-identical** to canonical `hero-bam-bag-portrait-v3.webp`. So no stale/non-canonical brand media to replace. **Decision (documented for owner):** the per-drop cuisine **cover art** (`/art/cover-*.svg`, flat illustrations) is left **as-is** — it is content-layer, and both §25 ("do NOT substitute the brand hero for food cards") and the session rule ("no fabricated food photos implying specific dishes", §14/§24) forbid the two obvious swaps; real licensed food photography stays **owner-deferred** (pre-commercial-launch, §25 changelog 2026-07-05(7)). The **one genuine on-spec gap**: the shared `@gozaika/ui` `EmptyState` (all web empty states + `not-found`) was a text-only dashed card — exactly the "reads as pre-content" feel the audit flagged. Added the **canonical flame motif** to it (`brandAssets.mark` → `/brand/gozaika-mark.svg`, decorative `alt=""`/`aria-hidden`), implementing the §25 "flame as motif on empty/loading moments" directive centrally. **Verified in preview** on `not-found.tsx`: flame renders (naturalWidth 512, `complete:true`, correct src, 40×40 @ 0.8 opacity) — screenshot confirms the saffron BAM flame leads the card. a11y: decorative image, out of the a11y tree; axe still green.
- **Phase 5 Playwright specs — DONE + PASSED against live remote.** Added two **opt-in authed** consumer-web specs (gated behind `RUN_AUTHED_SMOKE=1`, so the default web-ci run skips them → gate stays 10/10; same pattern as the `RUN_AUTHED_A11Y` test already in `a11y.spec.ts`): `tests/allergen-gate.spec.ts` (§16 — Rahul VEG × a Non-Veg drop → press Hold → assert the `alertdialog` interstitial + "Claim anyway"/"Cancel", then Cancel; **non-mutating**) and `tests/order-again.spec.ts` (§20 — open a COLLECTED order → assert the full-price `ReorderCard` copy + "Order again" button; does **not** click it → **non-mutating**). Shared `tests/auth-helpers.ts` (not a `.spec`, so Playwright ignores it) logs in via **phone OTP** (Rahul `+919876510002`/`100002`) and returns a boolean so callers `test.skip` on a mis-seeded env instead of hard-failing. Wired both into the `e2e` script (skip by default) + added an `e2e:authed` convenience script. **Ran them with `RUN_AUTHED_SMOKE=1` against live remote → both PASSED (2 passed, 14.1s)** — genuinely exercised (login + Non-Veg drop found + interstitial; COLLECTED order found + ReorderCard), not skipped.
- **Gates:** web-ci **10/10**, mobile-ci **7/7** (both full runs, this session). No Metro running → no web-ci/Metro-watcher conflict.
- **Deliberately NOT done (documented):** (a) Maestro mobile flows for the two new flows — **not added**: no warm consumer-mobile dev-client this session to verify selectors, and shipping unverified device flows fails the polish bar; left as a follow-up (the flows are already verified on-device in Session 6 evidence). (b) expired-holds release job — owner-deferred, untouched. (c) Did not re-seed remote (avoids the demo UTC-window bug + needs no migration); the authed specs passed on the current seed.
- **PR:** branch pushed (`838c027`). **PR could NOT be auto-created** — the `GH_TOKEN` PAT lacks "Pull requests: write" (`Resource not accessible by personal access token`). **Owner: open it here →** `https://github.com/gozaika/goz-website/compare/main...claude-phase3-strategy?expand=1` (ready-to-paste body saved in the session scratchpad: `PR-body-phase3-phase5.md`). **NOT merged** — owner authorization required before any merge to main (prod auto-deploy). NB: this branch's 5 append-only migrations are already applied to remote.

## Session 6 progress (2026-07-08) — §20 "Order Again" reorder BUILT + verified end-to-end
- **Order Again (§20) — DONE + verified, the last open Phase 3 item.** Owner-approved full MVP + authorized the remote migration. Design (reuse rails, no new ordering engine): the reorderable unit is the **bag template** (no dish SKUs exist); a reorder is an on-demand **PRIVATE (INTERNAL_ONLY), full-price, single-bag REORDER drop** of the same template revision for the restaurant's next pickup window, created + held server-side, then flowed through the **unchanged** hold→checkout(simulator)→pickup-OTP rails. Full price = `catalog_bag_template_revision.min_menu_value_paise`. The reorder→source link lives on `drop_drop.reorder_source_order_fk` (internal; never in public views) — that link **is** the §20 ROI instrumentation.
- **Migrations (both applied to remote, append-only):** `20260708000000_phase3_s20_order_again_reorder.sql` — `REORDER` drop type, `drop_drop.reorder_source_order_fk`, `api_create_reorder_drop(source_order, idempotency_key)` RPC (creates the private drop + reserves the hold atomically; auth via `rls_current_consumer_profile_pk()`), and the `api_restaurant_reorder_roi` view (per-restaurant sample→reorder ROI). `20260708010000_phase3_s20_reorder_counter_flag.sql` — adds `is_reorder` to `api_restaurant_pickup_order_summary`.
- **App:** types `ReorderResultDto`/`reorderResultSchema` + `RestaurantOrderSummary.isReorder` + `CounterOrder.isReorder`. BFF routes `POST /api/mobile/v1/reorder` + web `POST /api/reorder` (both call the RPC with the caller's token so `auth.uid()` resolves). Consumer entry: mobile `ReorderCard` + web `reorder-card.tsx` on the order detail, shown for **COLLECTED** orders ("post-taste"). Counter surfacing: portal + restaurant-mobile queues flag reorders ("↻ Reorder — full-price Order Again"); shared loader threads `is_reorder`. Reports: portal `/portal/reports` "Reorder ROI — the discovery funnel, measured" card (reorders / GMV / samples-converted / conversion %).
- **Verified end-to-end (2026-07-08, emulator Rahul + portal Sattvik):** collected order → **ReorderCard** (`4-D1`) → **Order again** → full-price checkout **₹220** vs ₹99 discounted (`4-D2`) → Confirm → real reorder order **`GZ-HYD-202607-000099`** (REORDER drop, INTERNAL_ONLY, CAPTURED). Remote data: Sattvik ROI = 7 samples / 1 reorder / 1 converted / ₹220 GMV. Portal **reports ROI card** shows "Reorders 1 · GMV ₹220 · Samples converted 1/7 · Conversion 14.29%"; portal **counter queue** shows the order with the "↻ Reorder" flag. web-ci **10/10**, mobile-ci **7/7**.
- **Follow-up (not built, flagged):** restaurant-mobile counter badge is code-complete but **not device-verified** (no warm restaurant-mobile dev-client this session). Playwright/Maestro specs for the reorder flow (Phase 5). Optional: mobile ROI screen could also surface reorder ROI (web portal done).
- **Phase 3 is now COMPLETE.** Remaining program work: Phase 4 (CW-3 real imagery, a11y sign-off) + Phase 5 (extend Playwright/Maestro, open PR — ASK before prod merge).

## Session 6 progress (2026-07-08) — mobile evidence: §16 gate + CM-1 + CM-2 CAPTURED
- **All 3 open consumer-mobile evidence flows captured on the emulator against live remote data.** Saved to `docs/screenshots/consumer-mobile/`, INDEX rows flipped to ☑, screen-id testIDs verified in uiautomator.
  - **§16 allergen gate — `3-A1__claim-allergen-gate.png`.** Logged in as **Rahul** (VEG, `+919876510002`/OTP `100002`) → opened Bawarchi **NON_VEG** "Hyderabadi Biryani BAM Bag" → **Claim a bag** → dietary-conflict interstitial ("Check this against your preferences / This bag is Non-veg, which doesn't match your saved dietary preference" + Cancel / Claim anyway). testID `screen:claim-allergen-gate` verified.
  - **CM-1 checkout simulator — `3-B1__checkout-simulated.png`.** "Claim anyway" created the hold → landed on the **simulated** checkout ("Demo · simulated payment" + gated-simulator copy + Confirm payment / Simulate failure). Confirms **PAYMENTS_SIMULATOR_ENABLED effective**: BFF `POST /api/mobile/v1/checkout/order` → 200 with `mode=simulated`. testID `screen:checkout-simulated` verified.
  - **CM-2 pickup proof — `4-A2__order-pickup-proof.png`.** Tapped **Confirm payment** → real server order (`GZ-HYD-202607-000098`, CONFIRMED, ₹149 paid) → order detail leads with the in-app **PickupProofCard** (QR grid + OTP `119389`, "goZaika stores hashes for verification"), **Resend by SMS** secondary. testID `screen:order-pickup-proof` verified.
- **Code (surgical, both gates green):** added an optional `testID` prop to the shared `@gozaika/mobile-ui` `Screen` (forwards to `SafeAreaView`) and tagged the 4 route roots — `screen:drop-detail`, `screen:claim-allergen-gate` (Modal content View), `screen:checkout-simulated`, `screen:order-pickup-proof`. web-ci **10/10**, mobile-ci **7/7**. On branch `claude-phase3-strategy` (not merged).
- **Env/tooling gotchas (documented in the consumer-mobile INDEX "CAPTURE NOTES"):** (1) `PAYMENTS_SIMULATOR_ENABLED` was missing from `apps/consumer-web/.env.local` — appended (local, gitignored); for a Vercel demo it must be a project env var (owner's call). (2) **Demo-tooling bug:** `demo_create_live_drops()` anchors the pickup window to `current_date 13:00–15:30` in **UTC**, so a seed run in the UTC afternoon/evening creates already-expired "live" drops (list shows "No active drops"); re-run after UTC-midnight or fix the function to use a forward offset. Flagged as a follow-up. (3) After a re-seed, force-stop + relaunch the dev client to bust the in-memory query cache.
- **Phase 3 remaining:** Order Again (§20) in Orders/counter queue — the last open Phase 3 item.

## Session 5 progress (2026-07-07) — Phase 3 §19 template archetype + allergen envelope DONE
- **§19 — DONE + verified (the one non-cosmetic change).** Audited the model first: the allergen **envelope** already existed (the `catalog_bag_template_allergen` union rows), as did dietary/portion + reusable copy — so the only NEW fields were an **internal item count** (serves ≠ item count) and a **drop-time internal fill note**. Owner decisions this session: (a) minimal drop-fill scope = free-text note + read-only envelope reminder, ship the `evaluateEnvelopeBounding` helper+tests but do NOT wire a per-drop allergen input (no structured slots yet); (b) apply the migration to remote this session.
- **Migration** `20260707000000_phase3_s19_template_archetype_envelope.sql` (append-only, nullable) applied to remote: `catalog_bag_template_revision.archetype_item_count` (int 1–20, on the immutable revision) + `drop_drop.internal_fill_note` (text ≤500). **Both internal-only — verified `api_public_drop_card` exposes neither** (guards §14 never-promise-a-count + §24 keep-the-surprise). Seed source kept in sync + remote demo revisions populated with realistic counts (4/3/6/5/3/3/5/3/4/3).
- **App:** zod (`archetypeItemCount` optional-coerced on template schema, `internalFillNote` on drop schema) + `PortalBagTemplate.archetypeItemCount` + `PortalDrop.internalFillNote`; loader `slice3.ts`; template + drop API write paths (POST + publish_revision + duplicate carry the count). Portal template form → "Composition guide — items per bag (internal)" field (never-shown helper) + allergen block **reframed "Allergen envelope"** (union that bounds per-drop substitution; widen via new revision). Portal drop form → read-only envelope chips + composition-guide readout + optional internal fill-note textarea; note echoes in Recent drops. Shared `evaluateEnvelopeBounding` (+6 tests → 21) in `@gozaika/utils/allergen-safety.ts`.
- **Verified** logged-in as Bawarchi owner (real remote creds): template guide/envelope framing render, library shows "~N items (internal)"; drops/new shows T01 envelope chips DAIRY/WHEAT GLUTEN/NUTS + guide ~4; **end-to-end create persisted `internal_fill_note`** and rendered it in Recent drops; test DRAFT drop cleaned up; no 375 overflow; no console errors. web-ci **10/10**, mobile-ci **7/7**. Not yet committed at time of writing → commit on `claude-phase3-strategy`.
- **Phase 3 remaining:** Order Again (§20) in Orders/counter queue. Warm-env mobile captures still open (§16 allergen gate as Rahul, CM-1, CM-2).

## Session 5 progress (2026-07-07) — Phase 3 RP-1 (#418 hydration) DONE
- **RP-1 #418 hydration — FIXED + verified.** Root cause was timezone-local `Date` rendering in **client** components: on Vercel's UTC server the SSR HTML used UTC, the browser re-render used its own zone → React #418. **Reproduced deterministically** by running the portal dev server under `TZ=UTC` and driving it from a non-IST (America/Chicago) browser via the claude-in-chrome MCP — the only *unconditional* mismatch was `TemplateForm` on `/portal/templates` (`13 Jun` vs `14 Jun`); `orders-client:253` is a latent twin (only on a COLLECTED order's `collectedAt`). The audit's "every page" was over-stated — profile/dashboard/finance/reports/onboarding/drops verified **clean** (server-component dates don't hydrate; drops/new's `Date.now()` only seeds an input value React reconciles silently).
- **Fix (surgical):** `@gozaika/utils` now exports `IST_TIME_ZONE` + `istDayKey()` (2 new tests, 74/74); pinned `timeZone: "Asia/Kolkata"` on all 8 portal date renders (templates, orders, dashboard ×2 incl. the "today" revenue day-key, finance range, reports ×2, reviews). Formats unchanged → no visual restyle. **Bonus correctness fix:** UTC-server-rendered partner times were ~5.5h off (shown in UTC) — now always IST.
- **Verified:** `TZ=UTC` server + Chicago browser → templates renders stable `Created 14 Jun 2026`, **0 hydration errors** across all 9 portal routes; utils tests 74/74; **web-ci 10/10 green**. Not yet committed/pushed at time of writing → commit on `claude-phase3-strategy`.
- **RM-1 mobile ROI parity — FIXED + verified (commit `2242596`).** Not a window/query mismatch — both surfaces already share `loadRoiReport` + the same trailing-7-day `defaultRoiPeriod`. Real cause: the `api_restaurant_roi_*` (and finance settlement-summary) views self-filter on `rls_has_restaurant_access(auth.uid())`; the **web** portal reads them via the user **cookie client** (auth.uid()=owner → rows) but the **mobile BFF** read the same views via a **service-role client** (auth.uid()=null → 0 rows) with no base-table fallback → mobile ROI = ₹0/"0 drops". Mobile **finance** had the identical latent bug. Fix: run the shared loader + finance query with a **bearer** client (`createServerSupabaseClient(parseBearerToken(...))`), already validated + capability-gated by `withMobileRestaurantRole`. Also IST-pinned the ROI period label + mobile date renders. **Verified vs remote** (Bawarchi owner token → local BFF): mobile ROI = 3 drops, 4/32 bags, ₹596 GMV, ₹586.26 net — byte-identical to web `/portal/reports`; mobile finance = PAID settlement (6 orders, ₹929.90). web-ci 10/10, mobile-ci 7/7. **Note (follow-up, not a regression):** `defaultRoiPeriod` builds a UTC-day-aligned window; shown in IST the end label reads one day later (e.g. window ending 7 Jul shows "8/7") — this now matches what IST-device mobile already showed. IST-aligning the window itself (defaultRoiPeriod + parseRoiPeriod + dateInputValue, coherently) is a separate optional polish.
- **Phase 3 remaining:** §19 (template archetype + allergen envelope — the one non-cosmetic change), Order Again in orders/counter. Warm-env mobile captures still open: §16 allergen gate (login as Rahul), CM-1, CM-2. Both devices connected (Pixel 7a `3A021JEHN02437` + emulator).
- **Possible follow-up:** consumer-web/mobile may have the same TZ-local date-render pattern — not audited this session; grep client components for `toLocale*`/`Intl.DateTimeFormat` without `timeZone` before consumer go-live.

## Session 4 progress (2026-07-07) — merge to main + strategy-doc parity + Phase 3 start
- **MERGED TO MAIN (owner-authorized).** web-ci 10/10 green → `claude-feature-parity` merged into `main` (`2813a67`→`8668968`) and pushed → Vercel **prod deploy** triggered. Phase 1–2 (marketing, consumer web+mobile, tooling, demo-fn fix) is now live. New work continues on branch **`claude-phase3-strategy`** (off main).
- **Strategy docs brought to parity** (commit `06b9671`): `docs/strategy/business/master-business-document-v4.docx` — fixed brand error "Big Zayka Ayeega Maza"→"Bada Zayka Ayega Maza" (3×), retired "mystery = can't choose" positioning (keep the surprise), §6.2 naming → "chef's thali" (composition) descriptor. `docs/strategy/technology-specification-v4.md` — reframed the 4 consumer "mystery" strings, corrected §3.7B banned-words list to canon (fixed the contradictory surplus/rescued replacements), added product-framing cross-cutting standard, dated the stale App-Readiness table. (Edited the docx via the docx skill: unpack → edit XML → repack; `PYTHONIOENCODING=utf-8` needed for pack.py on Windows.)
- **Phase 3 STARTED — RP-2 DONE** (commit `023e029`): finance summary "Orders 0" fixed (seed omitted `order_count`; now calls `api_finance_recalculate_run_totals`; remote demo data corrected 0→6 via a PAID→DRAFT→recompute→PAID txn). See IMPLEMENTATION-PLAN Phase 3.
- **Phase 3 remaining:** RP-1 (#418 hydration on every portal page — gate time-dependent render behind a mounted flag / `suppressHydrationWarning` / server-compute), RM-1 (mobile ROI parity — reconcile with web `loadRoiReport`), §19 (template archetype + allergen envelope + reusable copy — the one non-cosmetic change; keep it light per §19 phase-1 caution), Order Again in orders/counter queue.
- **Still open from Phase 2 (env warm):** mobile §16 allergen gate (login as Rahul VEG × NON_VEG drop), CM-1 checkout simulator, CM-2 pickup proof — see the consumer-mobile block below.

## Session 3 progress (2026-07-07) — screenshot system + demo-fn fix
- **TASK A (screenshot library) — infra DONE.** `docs/screenshots/` with 5 app subfolders + master `README.md` (screen-id system + filename nomenclature `<order>-<flow><step>__<screen-id>.png` + capture workflow) + per-app `INDEX.md` registries (every screen's id/route/planned filename pre-seeded). Screen-id system PROVEN: `data-screen-id="drops-list"` on consumer-web drops root reads back identically from SSR + live DOM via documented `preview_eval`. Confirmed zero pre-existing screen-id attrs (no collisions). Owner decisions: **web captures via a Playwright harness** (preview_screenshot can't write repo files — still to build); mobile via `adb exec-out screencap`.
- **TASK B (demo cleanup fix) — DONE + applied to remote + verified.** See the "Seed-tooling bug — FIXED" block in IMPLEMENTATION-PLAN.md. `demo_prepare_for_demo(p_create_live_drops=>true)` runs clean + idempotent; 5 fresh live drops seeded on remote NOW (ready for Task C device pass). Memory `reference_remote_db_ops_gotchas` updated (workaround retired).
- **TASK C (mobile on-device verify) — UNBLOCKED; thali framing DONE, rest queued.** Root cause was a stale build (installed release APK = embedded pre-Phase-2 JS; prebuilt debug APK too old natively → `ExpoPushTokenManager` redbox). **Fixed:** rebuilt a fresh debug dev-client from `C:\tmp\gozaika-build` (`android/gradlew :app:assembleDebug`, 8m52s) + installed on Pixel 7a + emulator; loads current Metro JS, no redbox. **Working capture setup (use the EMULATOR for live data — API origin `10.0.2.2:3000` needs consumer-web BFF on host:3000 + Metro watch + `adb reverse tcp:8081`):** see `docs/screenshots/consumer-mobile/INDEX.md`.
  - ☑ **Thali framing verified + captured (emulator):** `2-A1__drops-list.png` (testID `screen:drops-list` VERIFIED in uiautomator + "10 live chef-curated thalis — …the lineup a surprise" copy) and `2-A2__drop-detail.png` ("Not a deal. A discovery." block + "discover the dishes"). Mobile screen-id system PROVEN end-to-end (testID → hot-reload → uiautomator → capture).
  - ☑ **Auth blocker RESOLVED + CM-3 verified.** Owner confirmed remote has test OTPs enabled → pointed mobile at remote via `apps/consumer-mobile/.env.local` (gitignored: `EXPO_PUBLIC_SUPABASE_URL`/`ANON_KEY` = CLOUD values, BFF origin kept `10.0.2.2:3000`), restarted Metro `--clear`. Logged in on emulator as Priya (`+919876510001`/`100001`) against remote; authed BFF data loads ("Following" on Bawarchi) — the "Please sign in to continue" mismatch is gone. **CM-3 captured:** `4-C1__drops-list-peekbar.png` (PeekBar floats above tab bar, list inset keeps cards clear).
  - ☐ **§16 allergen gate — needs RAHUL, not Priya.** Priya has NO dietary/allergen prefs on remote (verified: `consumer_allergen_preference` empty for her), so no conflict fires. Use **Rahul** (`rahul.demo@gozaika.dev`, VEG dietary; phone `+919876510002`/OTP `100002`) × a **NON_VEG drop** (e.g. Bawarchi "Hyderabadi Biryani BAM Bag") → claim → dietary-conflict interstitial (same trigger as the verified web gate). Then CM-1 checkout simulator (+confirm PAYMENTS_SIMULATOR_ENABLED effective) and CM-2 order pickup-proof (needs a completed order). Add testIDs to those route roots as captured.
- **NEXT:** finish §16 gate (login as Rahul) + CM-1 + CM-2 on the emulator; then Playwright web-capture harness + web Phase 2 evidence → Task D tests + Phase 3. **Env is WARM:** fresh dev-client on Pixel+emulator, Metro watch running (remote env), consumer-web BFF on :3000, remote seeded (25 drops), emulator logged in as Priya.

## Phase 2 progress (updated 2026-07-06, session 2) — consumer surfaces
Branch `claude-feature-parity` pushed (Vercel PREVIEW). Commits: `a1d3315` thali+CW-1, `8c23916` §16+CM-1, `6a42dc7` CM-2+CM-3. **Web gate 10/10 green.**

**Done + web-verified (hands-on preview, screenshots):**
- Thali/variety framing (web): drops list hero, active DropCard, blind + non-blind drop detail ("Not a deal. A discovery." block). No fabricated SKU counts (§14). Retired "Mystery Cuisine" → "A cuisine to discover". Responsive 375/320 clean. Playwright `thali-framing.spec.ts` 2/2.
- §16 allergen-conflict gate (web): warn + explicit-ack interstitial. Verified logged in as Rahul (VEG, rahul.demo@gozaika.dev / DemoPass@2026) on a NON_VEG drop → interstitial → "Claim anyway" → hold → checkout. Shared model `@gozaika/utils/allergen-safety.ts` (15 tests). No console errors.

**Done + typechecks (mobile code / BFF) — NEEDS on-device verification:**
- Thali framing mobile (DropCard, drops list header, drop detail).
- §16 gate mobile: BFF `/api/mobile/v1/account/safety-preferences` + `useSafetyPrefs` + Modal interstitial in drop detail.
- CM-1: honest non-dead-end fallback for the razorpay stub.
- CM-2: BFF `/api/mobile/v1/orders/[id]/pickup-proof` + `issuePickupProofForOrder` + `usePickupProof` + `PickupProofCard` (RN-Views QR + OTP). Order detail in-app proof primary, SMS secondary.
- CM-3: `PeekBarInset` context → Drops/Home/Account/Orders bottom padding + drop-detail sticky-bar lift.

**CW-1 — DONE + verified on remote/preview (owner-authorized apply 2026-07-06).** Migration applied to remote (`CREATE FUNCTION` + `GRANT`). Verified logged in as Priya: `/api/account/discovery-profile` now returns 7 tried cuisines and `/account/discovery` renders "score 41 · Spice Voyager · 7 of 13 cuisines explored" (was 0/score 20). Apply command for reference (needs `MSYS_NO_PATHCONV=1` on Git Bash): `MSYS_NO_PATHCONV=1 docker run --rm -v "<repo>/supabase/migrations:/mig" postgres:16 psql "$CLOUD_SUPABASE_DB_URL" -f /mig/20260706000000_cw1_consumer_tried_cuisines_rpc.sql`.

**BLOCKERS / next session:**
1. **Verify PAYMENTS_SIMULATOR_ENABLED effective on preview BFF** (CM-1) — hit the mobile checkout order endpoint, confirm mode=simulated.
2. ~~Seed-tooling bug~~ **FIXED 2026-07-07** (session 3) — `demo_prepare_for_demo(p_create_live_drops=>true)` runs clean; migration applied to remote. Use the documented call directly now.

**Next session focus:** (a) apply CW-1 migration + verify passport on preview; (b) mobile on-device pass (adb Pixel 7a `3A021JEHN02437` + emulator; Metro from real source hot-reloads JS) — verify thali/allergen-gate/CM-1/CM-2/CM-3 with screenshots; (c) Playwright authed allergen-gate spec (opt-in, reuse Rahul login) + Maestro specs; (d) then Phase 3 (restaurant surfaces).

## Current state (updated 2026-07-06)
- **Branch:** `claude-feature-parity`, based on current `origin/main` (`2813a67` Marketing-v2-polished-still — includes handoff doc + marketing pipeline). Docs commit `afd629b` brought `docs/audit/` across. Pushed to remote; Vercel PREVIEW deployments confirmed (gozaika/consumer/restaurant green; SSO-protected). `main` untouched.
- **Phase:** 1 COMPLETE (marketing + gozaika.in copy + calculator both surfaces). Phase 2 (consumer surfaces) is next.

## Owner correction pass (2026-07-06, post-preview-review) — DONE + verified
- Terminology: "chef's tasting thali" → "chef's thali" everywhere (content.ts, navigation.ts, page.tsx, layout.tsx, explainer, both banners). Home H1 now "A generous chef's thali. No menu. No algorithm." — fits exactly 3 lines (verified).
- Removed hard dish counts from consumer copy (owner: the count is only restaurant guidance, not a promise): "three dishes" → "dishes" (home hero + BamBag), "keep two a surprise" → "keep the rest a surprise"; explainer "Three dishes"→"A spread of dishes", reveal knob "…discover two"→"…discover the rest". Tier ladder ranges (House ~3 / Chef's 3–4) LEFT hedged — they differentiate the §22 tiers on purpose.
- HowItWorksFlow dashed connector was mis-centred (top-10 vs circle centre 64px) → `top-16`; verified diff=0 for both the 3-step home flow and 4-step /how-it-works flow.
- Calculator (BOTH site + portal): (1) fill-mix now shows a segmented proportion bar (forest/gold/saffron) + legend + "fills the rest" note, so semi-prep salvage is clearly visible; (2) the green results headline was a dense sentence → replaced with a scannable visual hero ("THIS DROP CREATES / {N} new regulars a week" + surplus/fresh chips + one quiet aggregator-contrast line). Kept the 4/6 metric cards (owner liked them).
- Gates re-green: website lint/typecheck/build clean, a11y 6/6, smoke 5/5, calculator e2e 2/2 (fixed a strict-mode selector: the new bar's aria-label contains "surplus", so the test now uses getByRole('slider',{name:/^Surplus/})). Portal lint/typecheck/build clean; planner browser-verified logged-in at 1280, bar+hero render, no overflow.

## Done + verified
- Git setup complete (branch + docs carried across).
- Tooling verified: adb (Pixel 7a `3A021JEHN02437` + emulator online), remote Supabase reachable via docker `postgres:16` psql (`CLOUD_SUPABASE_DB_URL`, 11 restaurants), npx/docker present.
- Brand palette: `packages/design-tokens/src/colors.ts` — saffron `#FF6B35` (customer), forest `#1A5C38` (restaurant), gold `#D4A017`, cream `#FFF8F0`, charcoal `#2D2D2D`; AA text companions saffronText `#B23C0E`, goldText `#7A5C00`.

## In progress
- Phase 1 is COMPLETE (banners + explainer + gozaika.in copy pass + the restaurant economics calculator on both site and portal). Next up: Phase 2 — consumer surfaces (web + mobile).
- Owner decisions logged: files under `marketing/` (per §13); A4 = 3 separate language pages.
- Login for hands-on portal/consumer verification (remote Supabase): dev servers target remote (nxvthewcwimrpjbzbcvx). The prefilled `.example` demo login does NOT exist on remote — use the real seed creds from `supabase/seed_demo/README.md` "## Login credentials" via the demo email/password fields (e.g. bawarchi.owner@gozaika.dev / DemoPass@2026) or phone+OTP (+919876520001 / 200001). Preview screenshots can hang on the Next dev overlay — remove it first: `document.querySelector('nextjs-portal')?.remove()`.

## Done + verified (restaurant economics calculator — §11.2)
- Shared math: `packages/utils/src/economics.ts` (`computeEconomics`, `DEFAULT_ECONOMICS_INPUTS`, `normaliseFillMix`), exported from utils index. ONE source of truth for site + portal so the numbers can't drift. `economics.test.ts` — 24 tests; the §5 worked example ties out exactly (₹90 food, ₹23 commission, ₹1.75 thin per-bag contribution); CAC/break-even semantics + degenerate-input robustness covered. Money in paise, rates as fractions 0–1.
- Site tool: `apps/website/components/calculator/RestaurantEconomicsCalculator.tsx` — sales-oriented two-panel "your drop / your economics" (§14), cost assumptions hidden at defaults, on `/for-restaurants` (#calculator) after the fill-spectrum section. Content in `forRestaurantsContent.calculator`. `tests/calculator.spec.ts` (renders + recomputes) 2/2 pass. QA'd desktop 1280 + 375, live recompute verified.
- Portal tab: `apps/restaurant-mgmt-web/app/portal/planner/{page.tsx,planner-client.tsx}` — fuller decision-support version exposing all cost assumptions; "Planner" nav item added to the Build group in `app/portal/portal-nav.tsx` (Calculator icon). Auth-guarded like other portal pages. Browser-verified logged in as Bawarchi owner (all 6 result cards correct, headline correct, live recompute, no overflow at 1280+375, no console errors). Added `/portal/planner` to the opt-in authed a11y suite (RUN_AUTHED_A11Y=1). Portal typecheck+lint+build green; smoke 1/1 (authed skip by design).
- Gate status: utils tests 57/57; website typecheck/lint/build clean, smoke 5/5, a11y 6/6, calculator e2e 2/2; portal typecheck/lint/build clean, /auth/login a11y pass. apps/website is NOT in web-ci.mjs (only consumer-web + restaurant-mgmt-web are).
- Owner-provided canonical brand assets (already wired into the site, confirmed no changes needed): `icons/flame.svg`, `icons/gozaika-logo.svg`, and the webp images in `apps/website/public/images/` (hero-bam-bag-v3, hero-bam-bag-portrait-v3, about-illustration-v3, restaurant-hero-v3). Reuse these — do not source new stock images for goZaika-branded surfaces without checking these first.

## Done + verified (Phase 1 marketing collateral)
- `marketing/banners/banner-restaurant-A4.html` — te/hi/en (3 A4 print pages), restaurant B2B acquisition. QA'd in preview: all 3 scripts render (Noto Telugu/Devanagari fallbacks), each page fits one A4, `@page A4` + page-break set. QR is a placeholder (swap production partner-signup QR before printing).
- `marketing/banners/banner-restaurant-A6.html` — compact handbill, EN + Telugu eyebrow. Fits one A6.
- `marketing/explainer/gozaika-explainer.html` — BAM brand + thali product + two-layer moral/product message + restaurant fill-the-gap spectrum. QA'd desktop 1280 (3-col) + 375 (single-col, zero horizontal overflow). Consumer/product layer is banned-copy clean; "surplus" appears only in the B2B restaurant section (allowed §15).
- Static preview via `.claude/launch.json` config `marketing-static` (python http.server :4599); added `website` config (port 3002, `npm --workspace @gozaika/website run dev -- -p 3002`) since apps/website has no prior launch.json entry (port 3000/3001 already used by consumer-web/restaurant-mgmt-web).

## Done + verified (gozaika.in copy pass — apps/website)
- Retired ALL "mystery" framing site-wide: `lib/content.ts` (hero headline/founderLine/body, bamBag body/callout, restaurantTeaser heading/body, howItWorksContent.subtitle), `app/layout.tsx` (description, OG description, JSON-LD, keywords), `app/page.tsx` metadata, `app/opengraph-image.tsx`, `app/how-it-works/page.tsx` metadata, `lib/navigation.ts` how-it-works nav description. Left blog essays and one skeptical customer testimonial quote untouched (editorial voice discussing the concept, not brand positioning).
- Homepage re-sequenced (`app/page.tsx`): `RestaurantTeaserSection` moved to right after `TrustBadgesSection` (was 6th, now 2nd section) — implements §8 "re-sequence so the anti-aggregator B2B story leads." Restaurant-teaser headline now reuses the banner's exact "Fill your quiet hours. Win repeat regulars." for cross-surface brand consistency.
- Added `forRestaurantsContent.fillSpectrum` (new content block) + a new section in `app/for-restaurants/page.tsx` (gradient bar forest→gold→saffron + surplus/blended/fresh-made copy) — implements §11 on the site, right after `kitchenFlow`.
- Fixed a found-in-passing bug (not in the original audit list): several `operationalNotes`/`operationalFaqs` entries on `/for-restaurants` were internal spec-instruction text that had leaked into live visitor copy (e.g. "the page should communicate clear settlement windows..." / "should be framed as..."). Rewrote as direct declarative copy. The no-show and volume-tier answers now reuse the real 200+ bags/month number already in `comparisonRows` — no new pricing invented.
- Did NOT touch: `comparisonRows` commission numbers (real, existing business data), for-restaurants hero/kitchenFlow/onboardingSteps/differentiators/brandProtection (already strong per audit §7), about page (already clean, no banned copy), FAQ page (already clean).
- Verified: `npm run typecheck` clean, `npm run lint` clean (1 pre-existing unrelated warning in layout.tsx), `npm run build` clean, `npx playwright test tests/smoke.spec.ts` 5/5 pass, `npx playwright test tests/a11y.spec.ts` 6/6 pass (zero violations, including the new fill-spectrum section). Note: apps/website is NOT part of the `web-ci.mjs` gate (that only covers consumer-web + restaurant-mgmt-web) — validated directly instead.
- Visually QA'd in Chrome preview: hero, restaurant-teaser (now 2nd section), BamBag section, for-restaurants fill-spectrum section, operationalNotes section — desktop + 375, zero console errors, zero horizontal overflow.

## Next step — Phase 2 (consumer surfaces, web + mobile), Opus 4.8/high-effort
Per §0 build sequence step 2:
1. Thali/variety framing on drops/detail; surface SKU count/variety; "know one, discover two" for reveal (BLIND_ADVENTURE) drops.
2. CW-1 passport-cuisines fix.
3. CM-1 mobile checkout via simulator (PAYMENTS_SIMULATOR_ENABLED — verify effective after deploy).
4. CM-2 in-app mobile pickup code (also de-risks SMS testing).
5. CM-3 holds/toast polish.
6. §16 allergen-conflict gate (wire customer dietary/allergen prefs to the claim flow).
Read the launch-readiness audit CW-*/CM-* findings before starting. Refresh seed (`demo_prepare_for_demo(p_create_live_drops => true)`) before hands-on device testing.
Note: an existing `marketing-source/restaurant-sales-kit/` (copy/en-v1.json) exists — reconcile/reuse rather than duplicate if it overlaps when touched.

## Gotchas / conventions
- Tokens not raw hex → banners define CSS vars from the canonical palette.
- Banned consumer copy: `leftover|stale|cheap|clearance|liquidation|food rescue|sample|surplus`. Waste-economics is **B2B/restaurant-only** (§15). Never promise a serving count (§14) — signal generous abundance.
- Brand = BAM/Zayka/Swaad (BAM = "Bada Zayka Ayega Maza"); thali = composition ("how"); "flight" retired; retire "mystery = can't choose".
- Windows MAX_PATH: native mobile builds only from `C:\tmp\gozaika-build`; JS iteration from real source via Metro.
- Keep web-ci 10/10, mobile-ci 7/7. App changes surgical. Ask owner before final merge to main.
- Seed refresh (`demo_prepare_for_demo(p_create_live_drops => true)`) before hands-on testing.
