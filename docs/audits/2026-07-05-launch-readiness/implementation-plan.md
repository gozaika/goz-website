# goZaika feature-parity — Implementation Plan (phased checklist)

**Status: COMPLETE — merged to `main` via PR #1 (`d8dae0d`, 2026-07-08).** Originally executed on branches `claude-feature-parity` (Phase 1–2, off `origin/main` = `2813a67`) then `claude-phase3-strategy` (Phase 3–5).
**Sources of truth (at the time):** `business-model-strategy.md` (§0 build sequence), `audit.md` (gaps CW-*/RP-*/MK-*/CM-*/RM-*), `docs/handoff/gozaika_handoff_v1.md` + `docs/web/w5-w7-autonomous-decisions.md` (anti-drift). See `follow-ups.md` for durable items left open at close.

Status key: ☐ todo · ◐ in progress · ☑ done+verified

---

## Phase 0 — Setup & tooling
- ☑ Create branch `claude-feature-parity` off current `origin/main`; carry the pre-existing `docs/audit/` folder across (since reorganized into `docs/audits/2026-07-05-launch-readiness/`) (commit `afd629b`).
- ☑ Verify tooling: adb (Pixel 7a `3A021JEHN02437` + emulator), remote Supabase (docker psql, 11 restaurants), npx, docker, `.env.local` CLOUD_*, palette tokens.
- ☐ Seed refresh `demo_prepare_for_demo(p_create_live_drops => true)` on remote — run right before hands-on device/web testing (keeps live drops fresh).

## Phase 1 — Marketing (banners + explainer → then gozaika.in + calculator)
- ☑ `marketing/banners/banner-restaurant-A4.html` — 3 print pages (te/hi/en), restaurant acquisition, §12 anti-aggregator + §15 B2B waste→revenue + §11 fill-spectrum + §22 tiers, forest accent. QA'd in Chrome preview (all 3 scripts render, fits one A4/page, print @page A4).
- ☑ `marketing/banners/banner-restaurant-A6.html` — condensed handbill (EN + Telugu eyebrow). QA'd (fits one A6).
- ☑ `marketing/explainer/gozaika-explainer.html` — §21 BAM brand + chef's tasting thali (§12) + two-layer message (§15) + fill-the-gap (§11). QA'd desktop 1280 (3-col) + 375 (single-col, no overflow). Consumer layer banned-copy clean; surplus only in B2B section.
- ☑ gozaika.in (apps/website) copy/positioning: retired all "mystery" framing (hero, metadata, OG image, nav, JSON-LD); hero/BamBag/how-it-works reframed to chef's-thali/variety/generous (§12/§14); homepage re-sequenced so RestaurantTeaserSection (anti-aggregator B2B) leads right after the hero (§8); restaurant-teaser headline reuses the banner's "Fill your quiet hours. Win repeat regulars." for cross-surface consistency; added `forRestaurantsContent.fillSpectrum` (§11) as a new section on `/for-restaurants` (gradient bar + surplus/blended/fresh copy); fixed a found-in-passing bug where several `operationalNotes`/`operationalFaqs` entries were internal spec-instruction text leaking into visitor copy ("the page should communicate...") — rewritten as direct declarative copy, reusing the real 200+ bags/month volume-tier number already in `comparisonRows` (no pricing invented). Verified: website lint/typecheck/build/e2e(5)/a11y(6) all green; homepage/for-restaurants QA'd in Chrome preview at desktop+375, no overflow, no console errors. Standardized brand images (icons/flame.svg, icons/gozaika-logo.svg, apps/website/public/images/*.webp) were already wired into hero/about/for-restaurants — no image changes needed this pass.
- ☑ Restaurant economics calculator (§11.2) — site + restaurant web app.
  - Shared pure math in `@gozaika/utils` (`economics.ts`, exported from index) — single source of truth for both surfaces; 24 unit tests, §5 example ties out exactly.
  - Site: `RestaurantEconomicsCalculator` on `/for-restaurants` (#calculator) — simpler two-panel sales tool (§14), cost assumptions at defaults; calculator.spec.ts e2e (2 tests). QA'd desktop+375, live recompute confirmed.
  - Portal: `/portal/planner` decision-support tab + "Planner" nav item (Build group) — fuller version exposing every cost assumption. Browser-verified logged-in (Bawarchi owner) at 1280+375, no overflow, no console errors; added to opt-in authed a11y suite. Portal build + lint + typecheck green.

## Phase 2 — Consumer surfaces (web + mobile) — code COMPLETE; mobile device-verify + CW-1 remote-apply pending
Commits on `claude-feature-parity`: `a1d3315` (thali + CW-1), `8c23916` (§16 + CM-1), `6a42dc7` (CM-2 + CM-3). Web gate 10/10 green; pushed for preview.
- ◐ Thali/variety framing on drops/detail — **web done+verified** (list hero, active card, blind + non-blind detail, responsive 375/320, no console errors; Playwright `thali-framing.spec.ts` 2/2). **Mobile code done+typechecks** (DropCard, drops list header, drop detail) — device-verify pending. No SKU counts surfaced (no count field exists + §14 bans promises → variety framed qualitatively). "Know one, discover the rest" done honestly per drop type (restaurant always shown → standard: "discover the dishes"; BLIND_ADVENTURE: "discover the cuisine"). Retired "Mystery Cuisine" → "A cuisine to discover".
- ☑ CW-1 passport-cuisines — **DONE + verified.** `supabase/migrations/20260706000000_cw1_consumer_tried_cuisines_rpc.sql` (SECURITY DEFINER RPC, in-function authz, correct two-hop join) applied to remote (owner-authorized). Verified as Priya: discovery API returns 7 cuisines; `/account/discovery` shows "7 of 13 cuisines explored", score 41 (was 0/20). `discovery-profile.ts` already handled the RPC-present path — no app change needed.
- ☑ CM-1 — **DONE + device-verified (2026-07-08).** razorpay stub dead-end replaced with honest fallback; **PAYMENTS_SIMULATOR_ENABLED verified EFFECTIVE** on the local BFF (`POST /api/mobile/v1/checkout/order` → 200 `mode=simulated`; emulator rendered the "Demo · simulated payment" branch). Flag was missing from `apps/consumer-web/.env.local` → appended (local/gitignored; set as a Vercel env var for preview/prod demos — owner's call). Evidence `docs/screenshots/consumer-mobile/3-B1__checkout-simulated.png` (testID `screen:checkout-simulated`).
- ☑ CM-2 in-app pickup code — **DONE + device-verified (2026-07-08).** Ran the full simulator checkout → real server order `GZ-HYD-202607-000098` (CONFIRMED/PAID) → order detail leads with the in-app `PickupProofCard` (QR grid + OTP `119389`), Resend-by-SMS secondary. Evidence `docs/screenshots/consumer-mobile/4-A2__order-pickup-proof.png` (testID `screen:order-pickup-proof`). Note: QR is a visual proof (OTP is the verifiable credential); real scannable QR = follow-up needing react-native-svg + native rebuild.
- ◐ CM-3 holds/toast polish — **code done+typechecks**: `PeekBarInset` context (tabs layout publishes footprint) consumed by Drops/Home/Account/Orders (bottom padding) + drop-detail StickyActionBar lift. Device-verify pending.
- ☑ §16 allergen-conflict gate — **web + mobile DONE + verified.** Web (prior). **Mobile device-verified (2026-07-08):** Rahul (VEG) × Bawarchi NON_VEG "Hyderabadi Biryani BAM Bag" → **Claim a bag** → dietary-conflict interstitial ("Check this against your preferences / This bag is Non-veg…" + Cancel / Claim anyway) → ack proceeds to the hold/checkout. Evidence `docs/screenshots/consumer-mobile/3-A1__claim-allergen-gate.png` (testID `screen:claim-allergen-gate`). BFF `/account/safety-preferences` + `useSafetyPrefs` + Modal interstitial; shared model `@gozaika/utils/allergen-safety.ts`.

**Phase 2 remaining:** (1) ✅ CW-1 migration applied; (2) ✅ simulator flag verified effective (2026-07-08, local BFF — `mode=simulated`); (3) ✅ mobile on-device verification of allergen-gate/CM-1/CM-2 captured (2026-07-08); thali (2-A1/2-A2) + CM-3 (4-C1) captured prior; **(was) BLOCKED on a fresh native dev-client build** (installed release APK = embedded pre-Phase-2 JS; prebuilt debug APK redboxes on missing `ExpoPushTokenManager`; build copy stale). Recipe in `docs/screenshots/consumer-mobile/INDEX.md` + memory `reference-mobile-devclient-build`. First mobile testID `screen:drops-list` embedded (source, unverified). (4) Playwright authed allergen-gate spec + Maestro specs; (5) ✅ seed-tooling bug fixed (above).

**Seed-tooling bug — ☑ FIXED + applied to remote (2026-07-07).** Migration `supabase/migrations/20260706120000_demo_cleanup_appendonly_fix.sql` (seed source kept in sync): (a) `raise_immutable_error()` guarded carve-out — permits DELETE + referential UPDATE only under transaction-local flag `app.demo_cleanup_active='on'` set solely by service_role-only `demo_cleanup_data` (real paths still immutable — verified); (b) fixed a pre-existing FK RESTRICT bug (`payment_order_intent.drop_inventory_hold_fk`) — Step 2b + Steps 5/6 now remove order_fk-null hold intents before Step 12; (c) hardened all four `demo_*` fns to service_role-only (`revoke from anon, authenticated` — default privileges had left them open). `demo_prepare_for_demo(p_create_live_drops=>true)` now runs clean + idempotent (5 fresh live drops). Workaround retired.

## Phase 3 — Restaurant surfaces (portal + mobile) — STARTED 2026-07-07
- ☑ **§19 template archetype + allergen envelope + reusable copy — DONE + verified.** Audit first established that the model already carried the envelope (the `catalog_bag_template_allergen` union rows — schema comment already frames them as "union of all SKUs + may-contain"), dietary/portion (`dietary_category_code`, `serves_min/max`), and reusable copy (`display_name`/`short_description`/`allergen_summary_text`/`included_item_hint_text`; AI copy-gen is §17 "not now"). The only genuinely NEW fields were the **internal item count** (serves ≠ item count) and a **drop-time internal fill note** — the one non-cosmetic change. Migration `supabase/migrations/20260707000000_phase3_s19_template_archetype_envelope.sql` (append-only, nullable, applied to remote): `catalog_bag_template_revision.archetype_item_count` (int, check 1–20, on the immutable revision so it versions with the archetype shape and travels with the exact revision a drop pins) + `drop_drop.internal_fill_note` (text, ≤500). Both **internal-only — never in any consumer view** (verified: `api_public_drop_card` exposes neither column, guarding §14/§24). Types/zod: `archetypeItemCount` (optional, coerced) on `createBagTemplateSchema`, `internalFillNote` on `createDropDraftSchema`, `PortalBagTemplate.archetypeItemCount` + `PortalDrop.internalFillNote`. Portal template form: "Composition guide — items per bag (internal)" field with never-shown-to-customers helper + allergen block **reframed as "Allergen envelope"** (declare the union; per-drop fill must stay inside it; widen via new revision — §16 tie). Portal drop form: read-only envelope-chip reminder + composition-guide readout + optional internal fill-note textarea; note surfaces back in Recent drops. Shared invariant `evaluateEnvelopeBounding(envelope, fill)` added to `@gozaika/utils/allergen-safety.ts` (+6 tests, 21 total) — encodes "same-envelope swap is always safe; out-of-envelope is flagged"; shipped for the AI-composition phase, deliberately **not** wired to a per-drop allergen input (phase-1 caution: no structured slots yet). Seed source + remote demo data populated with realistic per-template counts (4/3/6/5/3/3/5/3/4/3). Verified logged-in as Bawarchi owner: templates page shows the guide + envelope framing + library "~N items (internal)"; drops/new shows the T01 envelope chips (DAIRY/WHEAT GLUTEN/NUTS) + composition guide; end-to-end create persisted `internal_fill_note` to DB and rendered it in Recent drops; no overflow at 375, no console errors. web-ci **10/10**, mobile-ci **7/7**.
- ☑ Calculator / decision-support tab (restaurant web app) — done in Phase 1 (`/portal/planner`).
- ☑ **RP-1 #418 hydration — DONE + verified.** Root cause: client components rendered a `Date` in the *runtime's local zone* — SSR on Vercel (UTC) produced e.g. `13 Jun 2026` while the browser re-render (any other zone) produced `14 Jun 2026`, tripping React #418. Reproduced deterministically with a `TZ=UTC` dev server + a non-IST (America/Chicago) browser: the ONLY unconditional #418 was `TemplateForm` (`Intl.DateTimeFormat("en-IN")` with no `timeZone`) on `/portal/templates`; `orders-client` line 253 was a latent twin (fires only when a COLLECTED order renders its `collectedAt`). The audit's "every portal page" was an over-generalization — profile/dashboard/finance/reports/onboarding/drops were all verified clean (server-component date renders don't hydrate; drops/new's `Date.now()` default only sets an input `value`, which React silently reconciles). Fix (surgical, formats unchanged): added `IST_TIME_ZONE` const + `istDayKey()` to `@gozaika/utils` (+2 tests) and pinned `timeZone: "Asia/Kolkata"` on all 8 portal date renders (templates, orders, dashboard ×2 incl. the "today" day-key filter, finance range, reports ×2, reviews). This ALSO fixes a production correctness bug: on the UTC Vercel server, server-rendered pickup/settlement times were shown to Indian partners in UTC (~5.5h off) — now always IST. Verified: `TZ=UTC` server + Chicago browser → templates renders stable `Created 14 Jun 2026`, **zero hydration errors**; utils 74/74; web-ci **10/10**.
- ☑ **RP-2 finance "Orders 0" — DONE.** Root cause: the demo seed inserts the static settlement run (`…f00000000001`) with explicit totals but omits `order_count` (defaults 0), inserts 6 ORDER_GROSS entries, advances to PAID — never recomputing. Fix: seed now calls `api_finance_recalculate_run_totals(...)` right after inserting entries (run still DRAFT). Remote demo data corrected in place (`order_count` 0→6) via a PAID→DRAFT→recompute→PAID transaction (the locked-guard allows status-only changes; blocks the money/count fields). View `api_restaurant_finance_settlement_summary` projects `sr.order_count` directly → summary card now reads "Orders 6". Verified at data + view layer (RLS hides it from raw psql).
- ☑ **RM-1 mobile ROI parity — DONE + verified.** Root cause was NOT a window/query mismatch (both surfaces share `loadRoiReport` + the same `defaultRoiPeriod` trailing-7-day window). The `api_restaurant_roi_*` + finance settlement-summary views self-filter on `rls_has_restaurant_access(auth.uid())`; the **web** portal reads them with the user's **cookie client** (auth.uid()=owner → rows), but the **mobile BFF** read the identical views with a **service-role client** (auth.uid()=null → predicate fails → 0 rows) and, unlike the orders loader, has no base-table fallback → mobile ROI collapsed to ₹0 / "0 drops listed" (RM-1). The mobile **finance** route had the same latent bug (unaudited). Fix: run the shared loader + finance query with a caller-scoped **bearer** client (`createServerSupabaseClient(parseBearerToken(...))`); the token is already validated + capability-gated by `withMobileRestaurantRole`, so the predicate resolves exactly as on web. Also pinned the ROI `periodLabel` (web header + shared partner copy) + mobile date renders to IST. **Verified against remote** (Bawarchi owner token → local BFF): mobile ROI now returns 3 drops, 4/32 bags, GMV ₹596, net ₹586.26 — **byte-identical to the web `/portal/reports` page**; mobile finance now returns the PAID settlement (6 orders, net ₹929.90). web-ci **10/10**, mobile-ci **7/7**.
- ☑ **Order Again (§20) surfaced in Orders/counter queue — DONE + verified (2026-07-08).** Full-price reorder on the existing drop→hold→pay→pickup rails (see the Phase 4 §20 entry). Reorders appear in the portal + restaurant-mobile counter queues flagged "↻ Reorder — full-price Order Again"; portal `/portal/reports` gained a "Reorder ROI" card.

## Phase 4 — Cross-cutting
- ☑ **§20 Order Again reorder end-to-end + sample→reorder instrumentation — DONE + verified (2026-07-08).** Reuse-the-rails design (reorderable unit = bag template; no dish SKUs): a reorder is a PRIVATE (INTERNAL_ONLY), full-price (`min_menu_value_paise`), single-bag `REORDER` drop of the same template revision, created + held by `api_create_reorder_drop()` and flowed through the unchanged hold→checkout(simulator)→pickup-OTP rails. Reorder→source link on `drop_drop.reorder_source_order_fk` = the ROI instrumentation. Migrations `20260708000000` (REORDER type + column + RPC + `api_restaurant_reorder_roi` view) + `20260708010000` (`is_reorder` on the pickup queue view), both applied to remote. BFF `POST /api/mobile/v1/reorder` + web `POST /api/reorder`; consumer entry (mobile `ReorderCard` + web `reorder-card.tsx`) on COLLECTED order detail; portal + restaurant-mobile counter "↻ Reorder" flag; portal reports "Reorder ROI" card. **Verified:** collected order → Order again → full-price ₹220 checkout (vs ₹99 discounted) → real reorder order `GZ-HYD-202607-000099`; Sattvik ROI 7/1/1/₹220; portal ROI card 14.29% + counter flag. web-ci 10/10, mobile-ci 7/7. Screenshots `4-D1`/`4-D2`. Follow-up: restaurant-mobile badge not device-verified; Playwright/Maestro specs (Phase 5).
- ☑ **CW-3 real imagery — DONE + real cover PHOTOS, 2026-07-08.** Owner clarified restaurant product/cover imagery is EXEMPT from §14/§24 → replaced the 5 placeholder cuisine cover illustrations with real Wikimedia-Commons food photos (biryani/thali/grill/coastal/bakery), each visually vetted; web `cover-{key}.webp` (3 refs flipped `.svg`→`.webp`), mobile `cover-{key}.jpg` (5 requires flipped `.png`→`.jpg`); mystery cover kept as the cuisine-agnostic illustration (§24). Verified in preview; web-ci 10/10, mobile-ci 7/7. (Earlier finding retained:) Audit found the §25 brand marks/logos/hero **already canonical across all surfaces** (`gozaika-mark.svg` byte-identical to `icons/flame.svg`; horizontal/white logos same art; `hero-bam-bag.webp` byte-identical to `hero-bam-bag-portrait-v3.webp`) → no stale brand media to swap. Per-drop cuisine **cover illustrations left as-is** (content-layer; §25 forbids brand hero on food cards; §14/§24 + session rule forbid fabricated food photos; licensed photography owner-deferred). The one on-spec gap closed: added the **canonical flame motif** to the shared `@gozaika/ui` `EmptyState` (`brandAssets.mark`, decorative) per §25 "flame on empty/loading moments" — verified rendering on `not-found.tsx` in preview. See CONTINUE-HERE Session 7.
- ☑ **a11y pass — kept green.** New EmptyState flame image is decorative (`alt=""`/`aria-hidden`), out of the a11y tree; web-ci axe step still passes (10/10). No new UI beyond that; human sign-off remains deferred to owner.
- ☐ (Deferred to owner) schedule expired-holds release job — do not build, don't regress availability.

## Phase 5 — Test + deploy
- ☑ **Extend Playwright (web *.spec.ts) — DONE 2026-07-08.** Added opt-in authed consumer-web specs `tests/allergen-gate.spec.ts` (§16) + `tests/order-again.spec.ts` (§20) + shared `tests/auth-helpers.ts` (phone-OTP login). Gated behind `RUN_AUTHED_SMOKE=1` (skip by default → web-ci stays 10/10; matches the `RUN_AUTHED_A11Y` precedent). Wired into `e2e` (skipped) + new `e2e:authed` script. **Ran with the flag against live remote → both PASSED (2 passed, 14.1s), genuinely exercised (non-mutating: cancel/read-only).** thali-framing.spec.ts (prior) covers thali framing; calculator.spec.ts (website) covers the calculator.
- ◐ Extend Maestro (mobile) — **deferred (not added this session).** No warm consumer-mobile dev-client to verify selectors; shipping unverified device flows fails the polish bar. The §16/CM-1/CM-2/§20 flows are already verified on-device in Session 6 evidence. Follow-up when a dev-client is warm.
- ☑ **Green gates: web-ci 10/10, mobile-ci 7/7** (full runs, 2026-07-08) + the two new Playwright specs pass with `RUN_AUTHED_SMOKE=1`.
- ☑ **Merged to main.** Branch `claude-phase3-strategy` merged via PR [#1](https://github.com/gozaika/goz-website/pull/1) (squash commit `d8dae0d`, 2026-07-08), owner-authorized. Prod deploy triggered. Program CLOSED — see `../README.md` and `follow-ups.md` for the durable items intentionally left open.
