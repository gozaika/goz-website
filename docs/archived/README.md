# Archived Docs

This directory is intentionally non-authoritative. Files here are retained for provenance and audit
history only.

Use an archived file only when an active doc links to it for historical context. If a future slice
revives anything from this tree, move the revived content into the active docs first and link back
to the archived source as provenance.

