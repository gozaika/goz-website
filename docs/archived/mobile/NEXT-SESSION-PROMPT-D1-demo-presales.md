# Next-session kickoff prompt (D1 — demo/presales readiness)

Paste the block below into a fresh Claude Code session to resume. It is
self-contained; the authoritative state lives in git + the docs it references,
not in any prior session transcript.

---

Continue the goZaika mobile + web UX uplift. You own the whole monorepo (single-agent). **No drift: follow the established conventions and the authoritative docs; never re-derive from memory.** We are at the start of a fresh session by design (the D1d store-asset work overlaps a separate track and deserves clean context).

**Read first, in order:**
1. `docs/mobile/CONTINUE-HERE.md` — entry point; the "Mobile UX uplift overlay" section has live state.
2. `docs/mobile/plans/mobile-implementation-plan-v1.md` — slice tracker + per-slice completion records (see the **D1** record + the **X1 accepted deviation** note).
3. `docs/product/gozaika-mobile-ux-uplift-analysis-v1.md` §A5 — the D1 sub-slice table.
4. `.codex-artifacts/archive/launch-assets-pre-factory/gozaika-polish-v2/CURRENT_STATE.md` — the store-asset track to reconcile before D1d.

**Current state (verify with `git log --oneline -8` and `node scripts/mobile-ci.mjs` → expect 7/7):**
- `main` holds the consolidated uplift chain U1→X1. **Active branch: `codex/mobile-ux-uplift/d1-demo-presales`** — continue D1 here; it merges to `main` (fast-forward) only when D1 is complete.
- **D1 finding:** the demo seed (`supabase/seed_demo/`) is already rich (8 consumers, 5 restaurants with vivid headline+story copy, drops/orders/reviews/payments/finance/passport). D1 is an **imagery + store-asset** slice, not data-entry.
- **D1a done** (mobile) + **D1b done** (web): original cuisine cover art (biryani/thali/grill/coastal/bakery) generated by `scripts/demo-art/build-art.mjs` (SVG→PNG via sharp). The cuisine→cover resolver is shared: `@gozaika/utils#cuisineCoverKey(name, cuisineTags?)`. Mobile uses it in `apps/consumer-mobile/src/ui/mediaFallbacks.ts#coverFor`; web SVGs live in `apps/consumer-web/public/art/`, wired into the restaurant-detail hero + the shared `@gozaika/ui` `DropCard` banner + the "Closing soon" cards. D1a was verified on-device; **D1b web has NOT had an on-screen capture yet.**

**Your tasks, in order:**
1. **First — D1b web visual QA (outstanding):** `npm.cmd --workspace @gozaika/consumer-web run dev`, open `/drops` and `/restaurants/[slug]`, and confirm the hero overlay keeps white text readable and the cover banners crop well. Fix anything off, re-commit.
2. **D1c:** drop-type / blind-bag art variants (CHEF_SPECIAL, BLIND_ADVENTURE) layered on the cuisine cover.
3. **D1d:** store-listing screenshot cards composed from the now-vibrant states — **reconcile with `.codex-artifacts/archive/launch-assets-pre-factory/gozaika-polish-v2` first** (don't duplicate/conflict).
4. **D1e:** Maestro demo-flow polish + a capture manifest.

**No-drift rules (follow exactly):**
- Keep `node scripts/mobile-ci.mjs` **7/7**; typecheck every workspace you touch (`npm.cmd --workspace @gozaika/<pkg> run typecheck`).
- Use **real seeded/demo data only** — do not fabricate restaurants, prices, QR/OTP, order states, metrics, ratings, or claims. Cuisine art is abstract illustration (not a photo of any real restaurant's food); real uploaded media still wins via `ProductMedia`/the product-media pipeline.
- Imagery = **original SVG/vector art by you** (owner-approved); regenerate via `node scripts/demo-art/build-art.mjs`. If the owner later supplies licensed photos, swap them through the product-media pipeline.
- Customer (saffron) vs partner (forest) stay siblings, not identical. Accent **text** must meet WCAG AA — use `accentTextColor`/`onAccentTextColor` / the `*Text` tokens in `packages/mobile-ui/src/tokens/`. (X1 deviation: customer primary buttons use charcoal-on-saffron, owner-kept; flip point `onAccentTextColor()`.)
- Commit per sub-slice with the `Co-Authored-By: Claude Opus 4.8` trailer and update the D1 record + CONTINUE-HERE in the same commit. Pushing the `d1-demo-presales` branch is fine; merging D1 to `main` only when complete.
- Android visual QA (if needed): `pwsh scripts/android-preview-install.ps1 -App consumer-mobile -SkipSync -CaptureScreenshot` (copy changed files into `C:\tmp\gozaika-build` first). Note: cloud demo drops' pickup windows have passed — run `supabase/seed_demo/demo_prepare.sql` to roll them forward for a live-drops demo.

Start by confirming state (git log + gate), then do the D1b web visual QA.
