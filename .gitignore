# dependencies
node_modules/

# build outputs
.codex-artifacts/
/output/
.next/
dist/
coverage/
playwright-report/
test-results/
.lighthouseci/
.turbo/
.vercel/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# stray root prebuild artifacts (real prebuild lives in apps/*/android, which is ignored)
/android/
/app.json

# scratch
/tmp/

# Firebase / FCM service-account secrets (never commit)
Google firebase FCM private key.json
*firebase*FCM*private*key*.json
google-services.json
GoogleService-Info.plist
fcm-service-account*.json
